---
title: Setup
description: Install the CLI, an adapter, and a token
---

What you need before adding, authoring, or publishing facets. To just try a facet, the [Quickstart](/quickstart) is faster.

<Visibility for="agents">

> **Setting up the facet CLI? Read the CLI's own agent instructions first:**
>
> ```sh
> facet instructions usage   # install, adapters, and project setup, headless
> ```
>
> See [`facet instructions`](/cli/instructions).

</Visibility>

<Steps>
  <Step title="Install the CLI">
    <CodeGroup>
      ```sh curl
      curl -fsSL https://agentfacets.io/install | bash
      ```

      ```sh npm
      npm install -g agent-facets
      ```

      ```sh bun
      bun add -g agent-facets
      ```
    </CodeGroup>

    Keep it current with [`facet self-update`](/cli/self-update). The authoring commands need v0.24.0 or newer.

    ```sh
    facet --version
    facet self-update
    ```
  </Step>

  <Step title="Connect an adapter">
    An [adapter](/docs/learn/adapters) materializes facets into one AI tool. Install one per machine:

    ```sh
    facet adapter add claude-code   # or: opencode, codex
    facet adapter list
    ```

    <Note>
      Without an adapter, [`facet add`](/cli/add) opens a picker in a terminal and fails in CI.
    </Note>
  </Step>

  <Step title="Create a registry account and token">
    Adding public facets needs no account. A token unlocks publishing your own facets and reaching [private ones](/private-facets) you have access to.

    Sign up at [agentfacets.io](https://agentfacets.io), create a <Tooltip tip="A revocable credential you mint in the web UI to authenticate the CLI to the registry.">personal access token</Tooltip> in your account settings, then:

    ```sh
    facet login                     # interactive, stores the token
    export FACET_TOKEN=fct_pub_…    # or this, for CI and scripts
    facet whoami                    # confirm the signed-in identity
    ```
  </Step>
</Steps>

## Verify your setup

```sh
facet --version        # CLI is installed and recent
facet adapter list     # at least one adapter connected
facet whoami           # signed in
```

## Next steps

<CardGroup cols={2}>
  <Card title="Install facets" icon="download" href="/guides/install-facets">
    Add, pin, reinstall, and remove facets.
  </Card>

  <Card title="Create a facet" icon="hammer" href="/guides/create-your-first-facet">
    Scaffold, author, build, and verify.
  </Card>
</CardGroup>
