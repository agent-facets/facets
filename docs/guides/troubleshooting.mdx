---
title: Troubleshooting
description: Fix common facet CLI errors
---

Common errors, what causes them, and how to fix them. Each entry mirrors the CLI's own `error: … fix: …` output.

<AccordionGroup>
  <Accordion title='"Unknown command" (e.g. facet instructions / modify)'>
    **Cause:** your installed CLI predates the command. `facet instructions`, `facet modify`, headless `facet create`, and `facet build --verify/--json` require **v0.24.0 or newer**.

    **Fix:** update in place with [`facet self-update`](/cli/self-update), then re-run:

    ```sh
    facet self-update
    facet --version
    ```
  </Accordion>

  <Accordion title='"could not reach the registry"'>
    **Cause:** `facet search`, a registry `facet add`, and `facet publish` need network access to the registry. Offline, they [**fail closed**](/specification/commit#verify) rather than write an unconfirmed entry.

    **Fix:** check your connection and retry. Note that **local paths** (`./my-facet`) and **git sources** (`github:owner/repo`) resolve without the registry, so you do not need registry access to install/add from these sources.
  </Accordion>

  <Accordion title='"no adapters installed" when running facet add (non-interactive)'>
    **Cause:** a facet has to be materialized into at least one adapter, but none is connected. In a real terminal `facet add` shows a picker; in a non-interactive shell (CI, piped stdin) it exits with an error instead.

    **Fix:** install an adapter first with [`facet adapter add`](/cli/adapters/add):

    ```sh
    facet adapter add claude-code
    facet adapter list
    ```
  </Accordion>

  <Accordion title='"adapter incompatible" / "unsupported adapter API" (build, add, remove, install fail before writing)' id='adapter-incompatible'>
    **Cause:** every installed adapter declares the adapter API contract it was built against, and each CLI release supports an exact set — currently just `0.3` (the read-only planning contract). Bundles declaring `0.0`, `0.1`, or `0.2` wrote files themselves and owned their own rollback, so this CLI cannot offer them the guarantees it now makes about exact restoration and concurrency; they are unsupported. A bundle installed before API versioning has **no** declaration; others can carry a malformed declaration or disagree with the npm metadata they were selected by. All these cases fail closed: `facet build`, `facet add`, `facet remove`, and `facet install` stop **before** invoking any adapter method or writing project files.

    Upgrading the CLI across this boundary makes every previously installed adapter unsupported at once, including the first-party ones. Reinstall each — the listing prints the exact command.

    Membership is exact, never ordering: `0.2` is not "close enough" to `0.3`.

    <Note>
    This is a *loadability* problem. An adapter that loads may still [not configure MCP servers](#mcp-adapters), which is a different failure with a different fix.
    </Note>

    **Fix:** list your installed adapters to see each one's declared API and status, then run the reinstall command printed next to the incompatible entry:

    ```sh
    facet adapter list
    # my-adapter  api missing  unsupported — reinstall: facet adapter add my-adapter
    facet adapter add my-adapter
    ```

    Reinstalling selects the newest release compatible with this CLI. See [compatible resolution](/cli/adapters/add#compatible-resolution) for how selection works, and note that [`facet adapter remove`](/cli/adapters/remove) always works regardless of compatibility.
  </Accordion>

  <Accordion title='"unsupported archive format" on install'>
    **Cause:** the facet's archive uses a [`facetVersion`](/specification/archive) your CLI doesn't support. A newer facet may use a format published after your CLI; a corrupt or unknown archive fails the same way. The adapter API, archive `facetVersion`, and lockfile version are [independent axes](/specification/install) — this is the archive axis.

    **Fix:** update the CLI with [`facet self-update`](/cli/self-update). The error names the minimum supporting release when the format is known; when it isn't, update to the latest.
  </Accordion>

  <Accordion title='"unsupported lockfileVersion" on install'>
    **Cause:** `facets.lock` declares a version this CLI does not read. The common case is the withdrawn closed-alpha `1`, which is no longer parsed — the number is reserved for the eventual stable v1 schema, so it is not reused for a compatibility shim. A version newer than your CLI fails the same way.

    **Fix:** if the file declares `1`, delete `facets.lock` and run [`facet install`](/cli/install); the lockfile is regenerated from `facets.json` and re-verified from scratch. If it declares something newer, update the CLI with [`facet self-update`](/cli/self-update) instead — deleting a newer lockfile would discard resolutions your teammates committed. The adapter API, archive `facetVersion`, and lockfile version are [independent axes](/specification/install); this is the lockfile axis.
  </Accordion>

  <Accordion title='"integrity mismatch" / companion drift on install'>
    **Cause:** a materialized file no longer matches the [per-file integrity](/specification/lockfile) recorded in `facets.lock` — a file was edited or corrupted on disk, or the cache was tampered with. Install reconciles every file (including each skill companion) before writing and reports the exact drifting path.

    **Fix:** re-run [`facet install`](/cli/install) — it repairs a drifted file in place (reported as *repaired*) without touching files you added yourself. If a registry facet fails to reconcile against its lockfile integrity, the download is rejected rather than silently replaced; confirm the lockfile matches what you expect. A `RECONCILE_*` failure is different: the archive itself disagrees with the lockfile, so re-running will not fix it — delete `facets.lock` and re-run, or [`facet add`](/cli/add) the facet to update the entry.
  </Accordion>

  <Accordion title='"two or more facets want the same name"'>
    **Cause:** two installed facets each publish a contribution that would materialize under the same name. Skills and commands share one namespace, so a skill and a command can collide too. MCP servers collide only with other servers — never with a skill, command, or agent. Nothing is written — the check runs before any file is touched.

    **Fix:** in a terminal, [`facet install`](/cli/install#name-collisions) pauses and lets you keep, rename, or omit each one. In CI it fails and prints every claimant with the exact `facets.json` edit; make that edit locally, commit it, and CI will never see the collision again:

    ```json facets.json
    "team-tools": {
      "source": "1.*",
      "materialization": {
        "skills": { "planning": { "kind": "aliased", "as": "team-planning" } },
        "servers": { "filesystem": { "kind": "aliased", "as": "team-filesystem" } }
      }
    }
    ```

    Only *one* claimant needs to change — the names just have to differ from each other. A `servers` group needs `"manifestVersion": 0.2`. Two facets declaring the *same* server identically never collide. See [Materialization](/specification/materialization).
  </Accordion>

  <Accordion title='"invalid materialization alias"'>
    **Cause:** an `as` value in a `materialization` block does not satisfy the [asset-name grammar](/specification/manifest#asset-names): 1–64 characters of lowercase letters, digits, and hyphens, no leading or trailing hyphen, no consecutive hyphens, and no `/`. Server aliases use the same grammar.

    **Fix:** correct the alias in `facets.json` and re-run. Aliases are never auto-corrected — silently rewriting your chosen name would make the result unpredictable.
  </Accordion>

  <Accordion title='"lockfile cannot record materialization overrides" (frozen install)'>
    **Cause:** `facets.json` declares an alias or omission for an **asset**, but `facets.lock` is still at lockfile `0.2`, which has no field in which to record it. [`--frozen-lockfile`](/cli/install) refuses rather than silently ignoring your intent.

    **Fix:** run one plain `facet install` (without the flag) to migrate the lockfile to `0.3`, then commit it. CI will pass from then on.

    A `materialization.servers` override can never cause this: no lockfile version records server intent, by [design](/specification/lockfile#why-mcp-did-not-move-the-lockfile).
  </Accordion>

  <Accordion title='"facets.json says X, lockfile says Y" (frozen install)'>
    **Cause:** you changed an alias or omission in `facets.json` but never ran a normal install, so `facets.lock` still records the old disposition. Under [`--frozen-lockfile`](/cli/install) that is drift, exactly like an unrecorded version bump.

    **Fix:** run `facet install` without the flag, then commit the updated `facets.lock`.

    A stale **server** override reports as drift too, even though the lockfile records no server intent — the check compares your override against the servers the integrity-pinned facet actually declares. The fix is the same.
  </Accordion>

  <Accordion title='A materialization choice was dropped, or is rejected in CI'>
    **Cause:** an override names an asset or MCP server the resolved facet version no longer contains — usually the publisher renamed or removed it in an upgrade. A normal install prunes the stale override and tells you which one; a frozen install treats it as blocking drift, because it has no transaction in which to prune.

    **Fix:** nothing, if you saw the pruning notice — it is already resolved and `facets.json` was updated. In CI, run `facet install` locally without `--frozen-lockfile` and commit the pruned manifest.
  </Accordion>

  <Accordion title='"facets.json declares an unsupported manifestVersion"'>
    **Cause:** the manifest was written by a newer CLI. Versions are matched exactly, never by ordering, so a newer format is refused rather than guessed at. The current version is `0.2`; `0.1` and unversioned manifests are still read, and a successful non-frozen install migrates them.

    **Fix:** update with [`facet self-update`](/cli/self-update).

    A `servers` group inside a document that declares `manifestVersion: 0.1` is a *validation* failure, not a version failure — `0.1` cannot express server intent, so the group is rejected rather than tolerated. Run one non-frozen `facet install` to migrate the document to `0.2` first.
  </Accordion>

  <Accordion title='"MCP server configuration needs approval" in CI'>
    **Cause:** a facet declares [MCP servers](/cli/install#mcp-servers) that this machine has not approved, and the run has no terminal to ask in. Approval is machine-local, so a teammate's approval — or a green run on another machine — does not cover this one. Nothing was written.

    **Fix:** read the printed declarations, then either approve them all or refuse the ones you do not want:

    ```sh
    facet install --accept-mcp
    ```

    ```json facets.json
    "mcp-tools": {
      "source": "1.*",
      "materialization": { "servers": { "docs": { "kind": "omitted" } } }
    }
    ```

    The same flag works on [`facet add`](/cli/add) and [`facet remove`](/cli/remove).
  </Accordion>

  <Accordion title='Approval keeps being requested for a server you already approved'>
    **Cause:** approval is keyed by the effective server name **and** the declaration's exact content, and it is only banked by a run that fully succeeds. So you are asked again when the declaration changed (command, argument order, an environment value, transport, URL, or the effective name), when the previous run failed or was rolled back, or when this machine's install receipt predates MCP support and therefore carries no approvals at all.

    **Fix:** for a changed declaration, review and approve it — the change is the reason you are being asked. For a receipt that predates MCP support, one successful install records approvals from then on.
  </Accordion>

  <Accordion title='"selected adapters cannot configure MCP servers"' id='mcp-adapters'>
    **Cause:** the project has active MCP declarations and at least one selected adapter declares no MCP support. The failure lists every such adapter and happens before any approval or mutation.

    **Fix:** updating the adapter will not help — declaring no MCP support is a deliberate answer, not a missing feature. Either [`facet adapter remove`](/cli/adapters/remove) the adapter, or omit the server declarations so the project has no MCP work to do:

    ```json facets.json
    "mcp-tools": {
      "source": "1.*",
      "materialization": { "servers": { "docs": { "kind": "omitted" } } }
    }
    ```

    <Note>
    This is different from an adapter that [cannot be loaded at all](#adapter-incompatible). That one *is* fixed by reinstalling.
    </Note>
  </Accordion>

  <Accordion title="A tool's MCP config file cannot be read or written">
    **Cause:** an adapter could not parse, validate, or write its native project configuration — `.mcp.json`, `opencode.jsonc`, or `.codex/config.toml`. The usual cause is a hand edit that left the file syntactically invalid.

    **Fix:** open the named file and repair it, then re-run. The failure is detected while **reading**, before any asset is written, and the document is left byte-for-byte unchanged. If a write did fail partway through a larger operation, everything is rolled back — including restoring each config file's exact prior bytes, comments and formatting included.
  </Accordion>

  <Accordion title='"two selected adapters configure the same file"' id='mcp-shared-file'>
    **Cause:** two connected adapters manage the same MCP configuration file. Facets does not support that: each plans its change against the file as it stands, so whichever writes second would apply a plan computed from content the first already replaced. The run stops before approval and before anything is written.

    **Fix:** [`facet adapter remove`](/cli/adapters/remove) one of the adapters named in the message. Upgrading will not help — both adapters are working correctly, and which tool should own the file is a decision only you can make.
  </Accordion>

  <Accordion title="A tool's MCP config changed while the command was running">
    **Cause:** a run re-checks every adapter's MCP plan immediately before writing it, including one that had concluded there was nothing to do. Something changed the named file in between — your editor, the tool itself, or a concurrent command — so what would be written is no longer what you approved.

    **Fix:** re-run. Nothing was written, and the second run plans against the file as it now stands. This is not an adapter bug: the adapter re-read the file and reported what it found.
  </Accordion>

  <Accordion title='"some files could not be returned to their previous state" after a failure'>
    **Cause:** the command failed and rolled back, but at least one file could not be put back. Every change is recorded as an exact `before → after` pair, and rollback classifies each file against what the run actually left there. The report names each one:

    ```
    Some files were changed by something else while this ran and were left as they are:
      .claude/skills/review/SKILL.md — changed by something else after this run wrote it; left as it is
      (7 other file(s) were restored)
    ```

    The two kinds of line mean opposite things:

    - **"changed by something else … left as it is"** — nothing is wrong with your file. Your editor, another agent, or a concurrent command wrote it after this run did, so the rollback declined to overwrite that work. Their content is intact.
    - **"could not be restored: …"** — the restore itself failed, usually a permissions or disk problem. This one needs you.

    **Fix:** for a preserved edit, nothing — decide whether you still want the change and re-run `facet install` when you do. For a genuine restore failure, resolve the named cause and re-run; install reconciles each file against its recorded integrity and repairs what it owns without touching files you added yourself.

    <Note>
    Facets never prompts to force-overwrite a contested file, in a terminal or in CI. Being asked to choose between two versions of a file during a failure — before you can compare them — is not a decision anyone can make well.
    </Note>
  </Accordion>

  <Accordion title='A takeover prompt appeared, or an install was cancelled at one'>
    **Cause:** something already occupies a name your project wants, and this machine's receipt does not record putting it there. For an MCP server this is disclosed up front on the [approval screen](/cli/install#taking-over-an-existing-entry); for a file it is a separate mid-write prompt with **Continue** selected by default.

    **Fix:** continue to take ownership, or cancel. Cancelling an asset takeover rolls back everything the command has done so far and exits non-zero — nothing is left half-applied. Non-interactive runs never see the asset prompt; they continue automatically, which is the behavior that predates the prompt.
  </Accordion>

  <Accordion title='A facet fails to install with an invalid "servers" entry'>
    **Cause:** the facet declares MCP servers in a form no longer accepted. Earlier speculative forms — a version string, or an object with `image` — are rejected in both current and legacy manifests, with no fallback and no warning. A declaration must be a concrete [`stdio` or `http` object](/specification/manifest#mcp-server-declarations), and it may not carry members outside that shape.

    **Fix:** if you author the facet, republish it with a concrete declaration. If you consume it, ask the publisher for a release that does. There is no flag to install it as-is: the whole point of rejecting the old forms is that nobody can be unsure what a declaration means.
  </Accordion>

  <Accordion title="A configured MCP server does not appear in your tool">
    **Cause:** Facets writes configuration; it does not run servers. A successful install proves the entry is in your tool's config file — not that the server starts, is reachable, or is authenticated.

    **Fix:** check the file the adapter wrote, then take it up in the tool. Many servers need a sign-in step the tool provides; Facets deliberately stores no credentials and performs no OAuth.
  </Accordion>

  <Accordion title='"not signed in — no registry credential found"'>
    **Cause:** `facet publish` (and `facet whoami`) need a registry credential and none was found.

    **Fix:** authenticate with [`facet login`](/cli/login), then retry. In CI, prefer the [`FACET_TOKEN`](/cli/env) environment variable:

    ```sh
    facet login              # interactive
    export FACET_TOKEN=fct_pub_…   # or, for CI/scripts
    facet whoami             # confirm the identity
    ```
  </Accordion>

  <Accordion title='Publish rejected: version already exists (HTTP 409)'>
    **Cause:** published `(name, version)` pairs are [**immutable**](/specification/publish#immutability) — you can't republish a version with different content. The status table lives at [`facet publish`](/cli/authoring/publish#publish-outcomes).

    **Fix:** bump the version, rebuild, and publish again:

    ```sh
    facet modify facet --version 1.2.0
    facet build
    facet publish
    ```
  </Accordion>

  <Accordion title='Build fails: skills["0"] must be an object'>
    **Cause:** `skills` is written as an array of strings. It must be a **map** of name → descriptor.

    **Fix:**

    ```json
    {
      "skills": {
        "code-review": { "description": "Guidelines for reviewing code changes" }
      }
    }
    ```

    Then re-check with `facet build --verify`. See the [Manifest reference](/specification/manifest).
  </Accordion>
</AccordionGroup>

## Still stuck?

Ask in the [community Discord](https://discord.gg/qXQYaYna5w) or open an issue on [GitHub](https://github.com/agent-facets/facets).
