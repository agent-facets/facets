---
title: "Introduction"
description: "The open source Agent Facet specification"
---

Facets are an open format for modular AI assistant extensions. A facet packages assets, which are skills, agents, commands, and MCP server declarations, into a versioned, distributable unit.

This specification defines the requirements an implementation must satisfy to produce or consume facets. For introductory material, see [Key Concepts](/docs/learn). Terms are collected in [Terminology](/specification/terminology).

Because a facet injects text into an assistant's context and its server declarations can make a tool run commands, [content verification](/specification/integrity) and [execution consent](/specification/install#mcp-configuration) are part of the core contract rather than optional hardening.

## Project state

A consuming project holds two version-controlled files.

<CardGroup cols={2}>
  <Card title="facets.json" icon="files" href="/specification/project-manifest">
    The project manifest: which facets the project depends on, and how it wants their assets materialized.
  </Card>
  <Card title="facets.lock" icon="lock" href="/specification/lockfile">
    The lockfile: what those dependencies resolved to, with verified integrity and per-file records.
  </Card>
</CardGroup>

The manifest records intent and the lockfile records resolution. Both are committed. An [install](/specification/install) also writes a machine-local ownership record, which is deliberately not.

## The facet artifact

A facet is a named, versioned collection of assets: [text assets](/specification/manifest#text-assets) and portable [MCP server declarations](/specification/manifest#mcp-server-declarations), described by a [facet manifest](/specification/manifest). It is built into a self-contained [archive](/specification/archive), and consumers receive exactly the bytes the author built.

<CardGroup cols={3}>
  <Card title="Facet Manifest" icon="file-code" href="/specification/manifest">
    `facet.json`: identity, assets, and declarations.
  </Card>
  <Card title="Facet Archive" icon="package" href="/specification/archive">
    The `.facet` container and its content rules.
  </Card>
  <Card title="Integrity Model" icon="shield-check" href="/specification/integrity">
    The hash domains, and when each MUST be verified.
  </Card>
</CardGroup>

Throughout this specification, uppercase key words such as MUST, MUST NOT, and MAY carry the meanings defined in [RFC 2119](https://www.ietf.org/rfc/rfc2119.txt) and [RFC 8174](https://www.ietf.org/rfc/rfc8174.txt). The same words in lowercase are ordinary prose.
