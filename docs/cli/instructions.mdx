---
title: facet instructions
description: Print CLI usage instructions for AI agents
---

## Usage

```sh
facet instructions [topic]
```

Prints agent-oriented workflow guidance: how an autonomous agent should author and use facets. For per-flag help, use `facet <command> --help`.

The default topic is `overview`, which opens with an index generated from the CLI's own topic list, so it always matches the topics the CLI serves.

<Note>
  Requires v0.24.0 or newer. If this reports "Unknown command", run [`facet self-update`](/cli/self-update).
</Note>

## Examples

```sh
facet instructions            # overview
facet instructions manifest   # facet.json structure and generated JSON Schema
facet instructions authoring  # how to author a facet non-interactively
facet instructions usage      # how to use facets in a project
```

## Exit codes

| Code | Meaning |
| --- | --- |
| `0` | Instructions printed. |
| `1` | Unknown topic. The valid topics are listed. |
| `2` | An unexpected error escaped command handling. |

## Output

<ResponseField name="overview" type="default">
  What facets are, the three artifact files, the two workflows, a command index, and a generated index of every topic.
</ResponseField>

<ResponseField name="manifest" type="topic">
  The `facet.json` structure, including top-level and per-skill supplementary `files`, plus the JSON Schema generated from the schema definition.
</ResponseField>

<ResponseField name="authoring" type="topic">
  Writing facets: scaffold, modify, verify, including README and supplementary-file authoring, MCP server declarations, and per-text-asset adapter configuration.
</ResponseField>

<ResponseField name="usage" type="topic">
  Project management: adding, updating, and removing facets, the shape of `facets.json`, resolving a name collision without a TTY, approving or omitting MCP servers without a TTY, and installing adapter tooling.
</ResponseField>

## Details

### The manifest schema is generated

The `manifest` topic appends the `facet.json` JSON Schema generated at runtime, so it always matches the CLI you are running.

Rules the schema cannot express, such as needing at least one asset and keeping skill and command names distinct, are described alongside it. Run [`facet build --verify`](/cli/authoring/build) to check a real facet.

## See also

- [`facet create`](/cli/authoring/create)
- [`facet modify`](/cli/authoring/modify)
- [`facet build`](/cli/authoring/build)
- [`facet adapter add`](/cli/adapters/add)
