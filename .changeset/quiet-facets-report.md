---
'agent-facets': minor
---

Add `facet update --json` for machine-readable update results and `facet outdated` for read-only update reports. Failed JSON runs include recovery diagnostics in the error document and leave stderr empty.
