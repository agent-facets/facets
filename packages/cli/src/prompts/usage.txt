facet — usage instructions for AI agents

"Using" facets means managing them inside a consuming project: adding,
updating, and removing facets, plus installing the adapter tooling that lets
facets target specific AI coding tools. This is project management, distinct
from AUTHORING (writing facets — see: facet instructions authoring).

Add a facet to a project — `facet add`
  Adds an entry to the project's facets.json and installs it immediately.
    facet add <source> [more sources...]
  Use --verbose for detailed step output on stderr. On failure the install is
  rolled back automatically.

  Use --accept-mcp when a facet declares MCP servers and you have no TTY (see
  "MCP servers" below). It is also accepted by `facet install` and
  `facet remove`.

  PREFERRED: the facet registry (agentfacets.io)
  The registry is the default, first-choice source. A bare name resolves to a
  published registry facet — reach for this first:
    facet add viper-plans                Latest published version.
    facet add viper-plans@1.2.3          An exact version.
    facet add viper-plans@1.*            Latest 1.x.
    facet add viper-plans@1.2.*          Latest 1.2.x.
    facet add viper-plans@latest         Explicit latest (same as bare name).
    facet add @julian/cowsay             A scoped registry facet.
    facet add @julian/cowsay@2.0.0       Scoped, exact version.
    facet add acme/cowsay                A namespaced registry facet.

  Other sources (use only when a facet is not on the registry)
    facet add github:owner/repo#ref      A facet in a GitHub repo at a git ref.
    facet add https://host/repo.git#ref  Any git URL at a ref.
    facet add git@host:owner/repo#ref    SSH git URL at a ref.
    facet add ./path/to/facet            A local facet directory in the project.

The project files
  facets.json – Your dependency list. `facet add` and `facet remove` maintain
                it; hand-edit it only to record materialization choices (below).
                It carries a "manifestVersion" the CLI stamps for you. The
                current version is 0.2, which is what a "servers" override
                group requires; 0.1 and unversioned documents are still read
                and are migrated by the next successful non-frozen install.

                Each entry is EITHER a source specifier string OR an object:
                  "facets": {
                    "cowsay": "1.*",
                    "team-tools": {
                      "source": "1.*",
                      "materialization": { ... }
                    }
                  }
                When reading or rewriting facets.json, handle BOTH forms. A
                tool that assumes every value is a string will destroy any
                recorded materialization choices.

  facets.lock – The install lockfile, managed by the CLI. Do not hand-edit.

Name collisions — when two facets want the same name
  Two facets cannot materialize a contribution under the same name. Skills and
  commands share one namespace; agents have their own; MCP servers are a third,
  separate space (a server never collides with a skill, command, or agent). The
  install detects this before writing anything and stops.

  Two facets declaring the SAME MCP server identically is NOT a collision —
  they agree, and one entry is configured.

  Without a TTY (CI, or any automated run) nothing is prompted: the command
  exits non-zero and prints every claimant with the exact facets.json edit.
  Resolve it by recording a choice per contribution, keyed by the AUTHORED name:

    "team-tools": {
      "source": "1.*",
      "materialization": {
        "skills": {
          "planning": { "kind": "aliased", "as": "team-planning" },
          "scratch":  { "kind": "omitted" }
        },
        "servers": {
          "filesystem": { "kind": "aliased", "as": "team-filesystem" }
        }
      }
    }

  Group keys are "skills", "agents", "commands", and "servers". The "servers"
  group requires "manifestVersion": 0.2 — it is REJECTED in a document that
  declares 0.1. Only two kinds are legal in any group: "aliased" (requires
  "as") and "omitted". Do NOT write { "kind": "authored" } — installing under
  the published name is expressed by recording NO override at all, and an
  explicit "authored" is rejected.

  An alias must satisfy the asset-name grammar: 1-64 chars, lowercase letters,
  digits and hyphens, no leading/trailing hyphen, no consecutive hyphens, no
  slash. Server names and server aliases use the same grammar. Invalid aliases
  are rejected, never auto-corrected.

  Only one claimant has to change — the names just have to differ. Re-run
  `facet install` after editing. There is no CLI flag for collisions; the
  resolver UI (TTY only) and hand-editing facets.json are the two ways to
  record a choice.

MCP servers — when a facet configures a server
  A facet may declare MCP servers. Installing one writes configuration into
  every connected tool telling it to launch a command or connect to a URL, so
  a new or changed declaration requires explicit approval first.

  Approval is MACHINE-LOCAL: it is recorded outside the project, so a
  teammate's approval (or a green CI run elsewhere) never covers this machine.
  It is banked only by a run that fully succeeds.

  Without a TTY, an unapproved declaration fails BEFORE anything is written and
  prints every server with its full declaration. You have exactly two ways
  forward:

    1. Approve everything reported:
         facet add <source> --accept-mcp
         facet install --accept-mcp
         facet remove <name> --accept-mcp

    2. Refuse specific servers by recording an omission (requires
       "manifestVersion": 0.2):
         "mcp-tools": {
           "source": "1.*",
           "materialization": { "servers": { "docs": { "kind": "omitted" } } }
         }

  --accept-mcp approves the COMPLETE set the command reports, including taking
  over an existing entry this project did not write. It does NOT resolve an
  asset name collision and does NOT approve an asset takeover.

  Other MCP failures you may hit, all before any mutation:
    - A selected adapter declares no MCP support. Updating will not help —
      remove the adapter or omit the servers.
    - A tool's native config file cannot be parsed. Repair the named file; it
      is left byte-for-byte unchanged.

  Do not put secrets in a declaration's "env" — those values are literal
  strings that ship inside the published facet. Tool-side authentication is
  the server's own concern; this CLI never launches, connects to, or
  authenticates to a declared server.

Adapter tooling — `facet adapter`
  Adapters teach facets how to target a specific AI coding tool. Installing an
  adapter is project setup, not authoring.
    facet adapter add <specifier>         Install an adapter.
    facet adapter list                    List installed adapters.
    facet adapter remove <name>           Remove an installed adapter.

  `facet adapter install` still works as a deprecated alias of `add`; it
  behaves identically and prints a deprecation notice on stderr.

  This CLI supports adapter API {{ADAPTER_API_SUPPORT_SET}}. An adapter that declares an older or
  unsupported API fails closed before any project files change. Note that
  loadability and MCP capability are separate questions: an adapter can load
  and work normally while declaring no MCP support, in which case it cannot
  configure MCP servers and reinstalling it will not change that.
  If `facet build`, `facet add`, or `facet install` reports an
  incompatible adapter:
    1. Run `facet adapter list` to see each adapter's API and status. Failing
       entries print the exact reinstall command to run.
    2. Reinstall an unsupported first-party adapter: facet adapter add <name>
    3. For a CUSTOM adapter, rebuild it against the current
       @agent-facets/adapter SDK first, then reinstall from that source.

  NOTE: This is about installing the adapter TOOLING. Configuring a facet's
  per-asset adapter settings (the `adapters` block in facet.json) is an
  AUTHORING task — see: facet instructions authoring
