jobs:
  - deploy-site:
      serial-group: << pipeline.project.slug >>/deploy-site
      context:
        - bot-context
        - slack-secrets
        - sst
      filters:
        branches:
          only:
            - main
