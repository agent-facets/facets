docker:
  - image: cimg/base:current
resource_class: small
environment:
  LEFTHOOK: "0"
  MISE_ENV: release
steps:
  - setup-mise
  - attach_workspace:
      at: .
  - run:
      name: Upload CLI release assets
      command: bun scripts/release-cli/upload-assets.ts
  - notify-failure
