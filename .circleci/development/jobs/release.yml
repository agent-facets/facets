docker:
  - image: cimg/base:current
resource_class: small
environment:
  LEFTHOOK: "0"
  MISE_ENV: release
steps:
  - setup-env
  - run:
      name: Publish
      command: bun scripts/release/publish.ts
  - notify-failure
