jobs:
  - check:
      context:
        - turbo-cache
      filters:
        branches:
          ignore:
            - main
  - main-pipeline:
      context:
        - turbo-cache
        - bot-context
        - slack-secrets
      filters:
        branches:
          only:
            - main
