jobs:
  - check:
      context: slack-secrets
      post-steps:
        - notify-failure
  - release:
      requires:
        - check
      context:
        - release
        - slack-secrets
      filters:
        branches:
          only:
            - main
            - release
      post-steps:
        - notify-failure
        - notify-success
