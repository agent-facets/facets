docker:
  - image: cimg/base:current
resource_class: small
environment:
  LEFTHOOK: "0"
  MISE_ENV: release
steps:
  - setup-mise
  - attach_workspace:
      at: .
  - run:
      name: Finalize release
      command: bun scripts/ci-release-finalize.ts
  - notify-failure
