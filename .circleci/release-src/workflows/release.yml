jobs:
  - release:
      context:
        - turbo-cache
        - bot-context
        - slack-secrets
      filters:
        tags:
          only:
            - /^@agent-facets\/.+@\d+\..+/
        branches:
          ignore:
            - /.*/
